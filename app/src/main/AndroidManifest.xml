<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/recycling"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/recycling"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat"
        android:usesCleartextTraffic="true"
        android:name=".RecyclifyApp">


        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.AppCompat" />

        <activity android:name=".ui.registration.RegistrationActivity"
            android:exported="false" />
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat" />
        <activity android:name=".ui.sell.SellActivity" />
        <activity android:name="BookingActivity" />
        <activity android:name="MyBookingsActivity" />
        <activity android:name="StatisticsActivity" />
        <activity android:name="WalletActivity" />
        <activity android:name="ProfileActivity" />

        <activity android:name="CompanyManagementActivity" />
        <activity android:name="CompanyListingActivity" />
        <activity android:name="PickupRequestsActivity" />
    </application>

</manifest>